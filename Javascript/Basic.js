//Using consle.log(value) to rpitn the value
console.log(value) // is used to print
//Like cpp here too heyhave const
const value = 2;
let -> is used to assign a variable
Similarly var is used too

#imp
//Javascript is a dynamic language which is that the value of the varaible can be chanegd at run time
//CPP is a static laguage althoguh the dynamic memory can be aces in the C
// Although in some laguage they are comletely static - 
This usually means that javascript is directly accesign heap memoery instad of static
